{
  "name": "zebra-proxy",
  "version": "1.0.0",
  "description": "A simple API to proxy print requests to Zebra printers via TCP or USB",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["zebra", "printer", "proxy", "api", "tcp", "usb"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "usb": "^2.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
